{% extends 'base.html.twig' %}

{% block body %}
    {% import "macro.html.twig" as macro %}
    {{ macro.back('donor_request_form') }}

    <div class="card card-lg bg-base-100 shadow m-4 mt-0">
        <div class="card-body">
            <h2 class="card-title mb-4">
                <span class="ti ti-user-heart text-3xl"></span>
                Uspešno ste se registrovali kao donator!
            </h2>

            <div>
                <div>Hvala vam što ste postali deo Mreže solidarnosti.</div>
                <div>Vaša podrška znači mnogo svim našim nastavnicima. 💙</div>
            </div>

            <div class="mt-4">Vaša "Instrukcije za uplatu" možete da vidite na <a href="{{ path('donor_transaction_list') }}" class="link link-primary link-hover">ovoj</a> stranici.</div>
        </div>
    </div>
{% endblock %}
